@page "/tournyShortcuts"


<PageTitle>Tournament.Help</PageTitle>

<MudContainer Class="my-5 mx-5" Style="width:500px">
  <h2>
    Shortcuts in tournament mode
  </h2>

  <MudSimpleTable Class="my-5">
    <thead>
      <tr>
        @foreach (var h in headings)
        {
          <th>@h.ToUpper()</th>
        }
      </tr>
    </thead>
    <tbody>
      @foreach ((string x, string y) in rows)
      {
        <tr>
          <td>@x</td>
          <td>@y</td>
        </tr>
      }
    </tbody>
  </MudSimpleTable>

</MudContainer>
<MudSpacer></MudSpacer>



@code {

  string[] headings = { "Shortcut key", "Description" };
  (string, string)[] rows = {
    ("Ctrl + v", "Validate your tournament.json"),
    ("Ctrl + r", "Run tournament"),
    ("Ctrl + c", "Stop tournament"),
    ("Ctrl + u", "Update tournament GUI"),
    ("Ctrl + p", "Create crosstable"),
    ("Ctrl + Alt + W", "Adjudicate: White wins"),
    ("Ctrl + Alt + B", "Adjudicate: Black wins"),
    ("Ctrl + Alt + D", "Adjudicate: Draw"),

  };

  protected override Task OnInitializedAsync()
  {
    return base.OnInitializedAsync();
  }

}
